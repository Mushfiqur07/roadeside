# MongoDB Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/roadside-assistance?retryWrites=true&w=majority

# JWT Secret Key (generate a strong random string)
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random

# Server Configuration
PORT=5000
NODE_ENV=development

# Client URL (for CORS)
CLIENT_URL=http://localhost:3000

# API Keys (if needed)
# NOMINATIM_API_KEY=your-nominatim-api-key (optional, Nominatim is free)

# Email Configuration (for notifications - optional)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password

# SMS Configuration (for Bangladesh SMS services - optional)
# SMS_API_KEY=your-sms-api-key
# SMS_API_URL=https://api.smsservice.com/send
